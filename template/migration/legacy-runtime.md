# Legacy Runtime Recipes

The exact, verified commands to build and run the legacy app and the probes in
THIS clone. The oracle is only trustworthy if it actually runs — record the
working recipe here (versions, flags, env) so a fresh session can reproduce it
without rediscovery.

## Toolchain

- <Language/runtime version, e.g. JDK 17 / Node 20 / Python 3.11>
- <Build tool + version>
- <Any required flags, env vars, or OS-specific notes>

## Build the legacy app

```
<exact build command(s)>
```

## Run one probe end-to-end

```
<exact command to run a probe and produce a fixture>
```

## Capture reference evidence (UI migrations)

```
<how to run the legacy UI and capture screenshots into migration/reference/>
```

## Known gotchas

- <e.g. line-ending issues, headless flags, --add-opens, native deps>
